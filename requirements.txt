# --- Core / async ---
httpx>=0.27,<1.0
python-dotenv>=1.0,<2.0

# --- Brain (LLM providers) ---
# NVIDIA NIM and Groq both expose OpenAI-compatible REST endpoints,
# which we call directly via httpx (async). No extra SDK required.

# --- Audio: STT ---
faster-whisper>=1.0,<2.0

# --- Audio: TTS ---
piper-tts>=1.2,<2.0

# --- Audio I/O (microphone capture / playback) ---
sounddevice>=0.4,<0.5
numpy>=1.24,<3.0

# --- OS interaction (later modules) ---
# pyautogui, python-docx, etc. added when those modules land.
